# 系统设置页 UX 优化 — 问题备忘录

> UX 优化过程中发现的功能缺失、逻辑缺陷及补充计划。

---

## 已发现的问题

### 场景策略页

| # | 问题 | 严重度 | 计划 |
|---|------|--------|------|
| 1 | 场景的检索字段（topK/threshold/reranker等）修改后没有"与全局对比"的差异提示，用户不知道改了什么 | 中 | B3 已做 → 加 Badge 标注继承/覆盖 |

### RAG 调试台

| # | 问题 | 严重度 | 计划 |
|---|------|--------|------|
| 2 | AB 对比的两轮运行使用相同参数（只是 question 加了 variant 后缀），无法真正体现不同配置的效果差异 | 高 | 待补充：增加 AB 配置差异表单（topK/threshold 可分别设） |
| 3 | 调试台无法保存运行结果到评测集，无法做回归测试 | 中 | 待补充：加"保存到评测集"按钮 |

### 评测与反馈页

| # | 问题 | 严重度 | 计划 |
|---|------|--------|------|
| 4 | 反馈项的状态流转（new → triaged → shipped）只能通过 Modal 创建时设置，没有内联的状态推进按钮 | 高 | 待补充：加"推进状态"下拉按钮在每个反馈卡片上 |
| 5 | 评测得分趋势图只显示当前数据，无历史对比 | 低 | 待补充 |

### 运行状态页

| # | 问题 | 严重度 | 计划 |
|---|------|--------|------|
| 6 | 告警阈值配置仅为 local state，刷新即丢失 | 中 | 待补充：持久化到 mock snapshot |

### 系统设置页

| # | 问题 | 严重度 | 计划 |
|---|------|--------|------|
| 7 | 语言选择只有"简体中文"一个选项 | 低 | 待补充：至少加 English |
| 8 | 渠道和通知的 Toggle onClick 为空函数，无法切换 | 中 | 待修复 |

### 知识库

| # | 问题 | 严重度 | 计划 |
|---|------|--------|------|
| 9 | 导入向导 Step 2 中场景字段应为可选择但实际是只读 | 高 | 待修复 |
| 10 | Source options（Notion/Web）是死的 disabled 按钮，无 tooltip 说明 | 低 | 已计划，优先级低 |

---

## 待补充计划

### 优先（高）

1. **RAG 调试台 AB 对比增强**：AB 模式开启后显示两套检索参数表单（topK/threshold），运行两次分别使用不同参数
2. **反馈状态流转**：反馈卡片上加下拉按钮推进状态（new → triaged → shipped），每个流转写 auditLog
3. **导入向导场景字段可编辑**：Step 2 场景从只读改为 select 下拉菜单

### 次优先（中）

4. **告警阈值持久化**：写入 mock snapshot 而非 local state
5. **渠道/通知 Toggle 修复**：Settings 页的 Toggle onClick 实现状态切换
6. **调试台保存到评测集**：运行结果可以另存为评测记录
7. **禁用选项加 tooltip**：Notion/Web source option 加 hover 说明"即将支持"

### 低优先

8. 语言选项扩展
9. 评测趋势历史对比

---

## 三个 Goal 执行记录

### Goal 1: ServiceHealth 布局对齐 + 更新时间统一
| 问题 | 处理 |
|------|------|
| ServiceHealthPage 使用 1.35fr/0.9fr 等非标准列宽 | 统一改为 grid-cols-2 标准网格 |
| 各页面缺少更新时间或不统一 | 全部页面 header 统一加入页元信息（评测/反馈条数、历史运行数、团队人数等） |
| RagConfigPage 更新时间在底部栏 | ✅ 保持，这是配置页面的合理位置 |

### Goal 2: 导航拆分
| 问题 | 处理 |
|------|------|
| AI 控制台下混入知识库、配置、监控、系统设置 | 拆为 4 个独立一级目录：知识管理 / AI 配置 / 质量监控 / 系统 |
| aiConsoleIconMap 不再需要 | 删除，各菜单项直接指定 icon |

### Goal 3: 客服工作台 UX
| 问题 | 处理 |
|------|------|
| 无页面说明，客服不知道页面用途 | 顶部加 header 卡片：说明 4 步流程 + 当前队列统计 + 当前所处阶段 |
| 客服看到复杂三栏布局无从下手 | header 中的流程描述和当前阶段 Badge 给予明确引导 |

---

## 处理记录（最终）

| # | 发现于 | 问题 | 状态 |
|----|--------|------|------|
| 1 | ScenarioModelConfigPage | 字段校验缺失 | ✅ |
| 2 | RagTestLabPage | AB 对比参数相同 | ✅ |
| 3 | EvaluationFeedbackPage | 反馈无法推进状态 | ✅ |
| 4 | ServiceHealthPage | 布局未对齐 | ✅ |
| 5 | ServiceHealthPage | 告警阈值不持久化 | ✅ |
| 6 | Settings | 渠道/通知 Toggle 无功能 | ✅ |
| 7 | Settings | 语言只有中文 | ✅ |
| 8 | KnowledgeBase | 导入向导场景只读 | ✅ |
| 9 | KnowledgeBase | Notion/Web 无说明 | ✅ |
| 10 | All pages | 更新时间不统一 | ✅ |
| 11 | Sidebar | AI控制台过于混杂 | ✅ |
| 12 | CustomerService | 无页面引导 | ✅ |
| 13 | RagTestLabPage | 无法保存到评测集 | ✅ |
